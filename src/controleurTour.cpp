#include "controleurTour.h"
#include "action.h"

/*
void ControleurTour::executerAction(Action* a) {
    a->executer();
    historique.push_back(a);
}

void ControleurTour::annulerDerniereAction() {
    if (!historique.empty()) {
        historique.back()->annuler();
        historique.pop_back();
    }
}

const std::vector<Action*>& ControleurTour::getHistorique() const {
    return historique;
}
*/